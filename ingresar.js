const boton = document.getElementById("cerrar");

boton.style.backgroundColor = "red";
boton.style.width="200px";
boton.style.height="200px";
boton.style.borderRadius="10px"; 
boton.style.border="none"; 
 

/*
// 1. Seleccionamos el elemento PADRE que ya existe en la página.
const contenedor = document.getElementById('contenedor');
// 2. Creamos el nuevo elemento HIJO que queremos añadir.
const nuevoItem = document.createElement('p');
nuevoItem.textContent = 'aprende desde donde quieras'; // Le damos contenido
// 3. Usamos appendChild para insertar el hijo dentro del padre.
contenedor.appendChild(nuevoItem);

*/